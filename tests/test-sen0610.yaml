# Every entity the sen0610 supports; validated by CI with `esphome config`.
substitutions:
  presence_pin: GPIO21
packages:
  base: !include _base.yaml
  radar: !include common-sen0610.yaml
uart:
  id: uart_bus
  tx_pin: GPIO17
  rx_pin: GPIO16
  baud_rate: 9600
