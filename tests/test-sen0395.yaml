# Every entity the SEN0395 supports; validated by CI with `esphome config`.
substitutions:
  presence_pin: GPIO21
packages:
  base: !include _base.yaml
  radar: !include common-sen0395.yaml
uart:
  id: uart_bus
  tx_pin: GPIO17
  rx_pin: GPIO16
  baud_rate: 115200
